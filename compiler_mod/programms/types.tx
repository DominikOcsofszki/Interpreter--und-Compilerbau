{
# ls.ls;
# import src.pack.ast.types_ast;
# import numpy as np;
# print(import numpy);
flo:= 1.1;
st:="strrrr";
ch:='\n';
# arr:=[1.,flo,st];
arr:=[flo,flo,flo,flo];
arr:=[0,1,2,3,4];
arr[3];
# arr:=[1.1,1.,2,"a"];
# arr:=[1,2];
ls:=(1,2,3,4);
# print(1,2,3,4,5);
print(ls);
l1:=ls[1];
l1[1];

head:= l -> l[0];
tail:= l -> l[1];
getIndexItem:= l -> {
    # t1:=tail(l)
    print(tail)
    # h1:= head(t1)
};
t:=tail(ls);
h1:= head(t);
t2:= tail(t);
# t2:= tail(h1)
# h1
getIndexItem(ls)
}
