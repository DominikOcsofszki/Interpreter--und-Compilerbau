# def p_expression_local(p):
#     'expression : local ID assign expression in expression'
#     p[0] = generator_local.LocalExpression(p[2],p[4],p[6])
#
# '''expression : if expression then expression
{
y:=1;
local y:=123 in fac:= x -> x*y;
y:=1231233;
print(fac(1));

# print(fac(1));
fac(fac(fac(1)))
}
