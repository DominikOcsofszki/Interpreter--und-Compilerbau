{
    g := local gauss := x ->
        if x = 0 then 0
        else x + gauss(x - 1) in
            gauss(10);
    f := local fac := x ->
        if x = 0 then 1
        else x * fac(x - 1) in
            fac(8)
}
